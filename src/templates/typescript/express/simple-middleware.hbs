import { Request, Response, NextFunction } from 'express';

export async function {{nameToCamelCase name}}(
  request: Request,
  response: Response,
  next: NextFunction,
): Promise<void> {
  next();
}
