import { FastifyInstance } from 'fastify';

import { {{nameToCamelCase name}}Router } from '../{{name}}';

describe('{{nameToCamelCase name }}Router test', () => {
  beforeAll(() => {
  });

  afterAll(() => {
  });

  it.skip('STUB', async () => {
    // Arrange
    
    // Act
    await {{nameToCamelCase name}}();

    // Assert
  });
});
